# Node modules and package manager logs
node_modules/
.pnpm-debug.log

# Next.js build output
web/.next/
web/out/

# ZK Circuits build output
# We ignore the contents of circuits/build, but explicitly track verification_key.json
circuits/build/*
!circuits/build/verification_key.json
*.zkey
*.ptau

# Rust / Soroban smart contract build target
contracts/target/

# Environment variables and secrets
.env
.env.local
.env.development.local
.env.test.local
.env.production.local
.env.example.local

# Agents
.agents/

