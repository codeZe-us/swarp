[workspace]
resolver = "2"
members = [
    "groth16-verifier",
    "zendswap-pool",
]

[workspace.dependencies]
soroban-sdk = "26.1.0"

[profile.dev]
overflow-checks = true

[profile.release]
opt-level = "z"
overflow-checks = true
lto = true
codegen-units = 1
panic = "abort"
